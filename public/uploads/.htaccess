# ============================================================
# Apache Security Protection for Uploads Directory
# Disables server-side script execution in public uploads
# ============================================================

# Disable directory browsing
Options -Indexes -ExecCGI

# Prevent PHP / scripts from executing
<IfModule mod_php7.c>
    php_flag engine off
</IfModule>
<IfModule mod_php8.c>
    php_flag engine off
</IfModule>
<IfModule mod_php.c>
    php_flag engine off
</IfModule>

# Block all script extensions
<FilesMatch "(?i)\.(php|phtml|php3|php4|php5|php7|phps|cgi|pl|exe|sh|py|jsp|asp|aspx|shtml|shtm|fcgi|fpl)$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

# Force default handler for all remaining files
SetHandler default-handler
